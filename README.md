# Sistema de Biblioteca (TypeScript)

Projeto de POO em TypeScript.

Dependências e execução (Windows - PowerShell):

Instalar dependências:

```powershell
npm install
```

Executar o sistema (CLI):

```powershell
npm run start
```

Rodar testes unitários (Jest):

```powershell
npm test
```